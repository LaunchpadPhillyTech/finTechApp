// Winter break Project Instructions 

// Add an event listener to the DOMContentLoaded event to ensure the script runs after the page content loads

// Select all filter buttons and transaction rows from the DOM using query selectors

// Iterate over the filter buttons and add a click event listener to each button
// When a button is clicked:
// 1. Retrieve the filter type (e.g., 'all', 'income', 'expense') from the data-filter attribute
// 2. Update the appearance of the active filter button by adding/removing an 'active' class

// Filter the transaction rows based on the selected filter type
// Use the data-type attribute of each transaction row to compare with the selected filter
// Display rows that match the filter type or all rows if the filter is 'all'
// Hide rows that do not match the filter type

// Optional: Use animations or transitions for a smoother filtering effect
